{
  "watch": ["server"],
  "ext": "ts,json",
  "ignore": ["server/dist", "node_modules"],
  "exec": "cd server && tsup --config tsup.config.ts && cd .. && cross-env NODE_ENV=development node dist/server/server.js",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
